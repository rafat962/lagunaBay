version: "3.8"

services:
    react-app:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: realstate
        ports:
            - "4000:80"
        restart: always
        volumes:
            - /app/node_modules
